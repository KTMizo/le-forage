# Prismic : le repository est défini dans prismic.config.json (le-forage)
# Uniquement si l'API est passée en privé :
# PRISMIC_ACCESS_TOKEN=

# Secret du webhook Prismic → /api/revalidate (même valeur que dans Vercel)
PRISMIC_WEBHOOK_SECRET=
